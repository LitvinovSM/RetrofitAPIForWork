# Конфигурационный файл для запуска.
#BaseURL - URL на котором тестим
general{
    baseURL = "https://reqres.in/api/"
    READ_TIMEOUT = 60
    CONNECT_TIMEOUT = 60
}
credential{
    defaultLogin = "eve.holt@reqres.in"
    defaultPassword = "cityslicka"
}

